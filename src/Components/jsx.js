function JSX() {
  return (
    <div>
      <h2>React JSX</h2>
      <p>
        JSX stands for JavaScript XML. JSX allows us to write HTML in React. JSX
        makes it easier to write and add HTML in React.
      </p>
    </div>
  );
}

export default JSX;
